<%= form_for(@word_type) do |f| %>
  <% if @word_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@word_type.errors.count, "error") %> prohibited this word_type from being saved:</h2>

      <ul>
      <% @word_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %><br>
    <%= f.text_field :Name %>
  </div>
  <div class="field">
    <%= f.label :Description %><br>
    <%= f.text_field :Description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
